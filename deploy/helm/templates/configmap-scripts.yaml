{{- if .Values.entrypointScripts }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stac-browser.fullname" . }}-scripts
  namespace: {{ include "stac-browser.namespace" . }}
  labels:
    {{- include "stac-browser.labels" . | nindent 4 }}
data:
  {{- range $name, $script := .Values.entrypointScripts }}
  {{ $name }}: |
    {{- $script | nindent 4 }}
  {{- end }}
{{- end }}
